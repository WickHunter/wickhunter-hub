// src/version.ts
// Single source of truth for the hub's version string; keep in lockstep with
// package.json. Reported by GET /api/health and the install-hub.sh smoke check.
export const HUB_VERSION = "0.2.1";
